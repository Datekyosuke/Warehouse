namespace WarehouseAccounting.Model.Enum
{
    public enum Status
    {
        ordered = 0,
        found = 1,
        purchased = 2,
        transit = 3, //on the way
        arrived = 4,
        archived = 5,
    }
}